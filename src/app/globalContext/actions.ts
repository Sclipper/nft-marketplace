import { SET_ITEM_1, SET_ITEM_2 } from './constants'
import { GlobalDispatch } from './GlobalContext.types'

export const globalActions = (dispatch: GlobalDispatch) => ({
  // updateTest1: (item1: number) => dispatch({ type: SET_ITEM_1, payload: item1 }),
  // updateTest2: (item2: string) => dispatch({ type: SET_ITEM_2, payload: item2 }),
})
